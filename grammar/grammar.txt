expr    : KEYWORD:sea IDENTIFIER EQ expr \\ lowest priority
        : term ((PLUS|MINUS) term)* 

term    : factor ((MUL|DIV|MOD) factor)*

factor  : (PLUS|MINUS) factor
        : power

power   : atom (POW factor)*

atom    : INT|FLOAT|IDENTIFIER \\ highest priority
        : LPAREN expr RPAREN